export type ThemeType = {
  theme: {
    color: string,
    background: string,
    link: string,
  }
};

const theme = {
  color: '#D5EBF2',
  background: '#152347',
  link: '#D110B8',
  spacing: '1rem',
}

export default theme;
